function aboutLogic(){
    bodyL.innerHTML = aboutDesign;
    bodyR.innerHTML = aboutHTML;
    rightColor.style.transform = "skew(0deg)"
    logoTextObj.inner.innerHTML = logoTextObj.about;
    logoTextObj.page.innerText = "About";
    aboutCircleCreation();
}